spring.application.name=PC3-CS1

# Database Configuration
spring.datasource.url=jdbc:postgresql://aws-1-us-east-2.pooler.supabase.com:6543/postgres
spring.datasource.username=postgres.kkkffeotqqbtjgtsphnb
spring.datasource.password=cPWiHR74BzwlAnL6
spring.datasource.driver-class-name=org.postgresql.Driver

# HikariCP Configuration (fix prepared statement issues)
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.auto-commit=true

# PostgreSQL specific settings
spring.datasource.hikari.data-source-properties.prepareThreshold=0
spring.datasource.hikari.data-source-properties.preparedStatementCacheQueries=0
spring.datasource.hikari.data-source-properties.preparedStatementCacheSizeMiB=0

# JDBC Configuration
logging.level.org.springframework.jdbc=DEBUG

# Cookie Configuration
app.cookie.name=PC3Token
app.cookie.secure=true
app.cookie.same-site=None
app.cookie.max-age=3600
app.cookie.http-only=true

# URLs
url.client=http://localhost:4200
url.site=http://localhost:8080